{
  "name": "projectx",
  "private": true,
  "version": "32.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "check": "node --check px-final.js && node --check projectx-core.js && node scripts/projectx-runtime-test.mjs && node scripts/projectx-forensic-regression.mjs && node scripts/backend-contract-test.mjs",
    "security:scan": "node scripts/security-scan.mjs",
    "test": "npm run check && npm run security:scan",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
