# V13 Release Notes

V13 is the GitHub Pages deployment cleanup release. It keeps the Universal Creation Engine UI while fixing GitHub Pages pathing, cache busting, setup visibility, demo-mode honesty, and provider connection guardrails.

The project still requires a real Supabase project/configuration before live Auth, encrypted BYOK storage, Edge Functions and AI provider calls can work.
